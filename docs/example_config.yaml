# UNION CONFIG EXAMPLE (YAML)
# It is also possible to use JSON format or plain Python dict
# in cases where Union is called from Python code.

# General section (required by client)


# Redis section (required by client and backend (worker))


# PostgreSQL section (required by backend (worker) and monitor)


# Optional monitor setup (example)
############################
# filters:
#   apps: [union]  # show only the apps from list
#   fields: [app, timestamp]  # show only the fields from list in the order defined
#   types: [error, debug]  # show only the types from list
#   loggers: [root]  # show only the loggers from list
